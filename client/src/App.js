import { Button, Input } from './components/index';

function App() {
  return (
    <div className="App">
      <Button text="ENTRAR" />
      <Input label="Nome" type="text" placeholder="Insira o nome aqui" />
    </div>
  );
}

export default App;
